# Archfrican identity shape — the owned base keeps the macOS-grade 8/12/16 radius family (the look the
# user likes) but INHERITS the ADL Inter / JetBrainsMono type from tokens.defaults.sh — no SF Pro/SF Mono
# dependency (those aren't on Linux; the macos-* clones only fell back to Inter silently). Deltas only.
export RADIUS_SM="8"
export RADIUS_MD="12"
export RADIUS_LG="16"
export RADIUS_WINDOW="10"
