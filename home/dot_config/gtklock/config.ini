# Archfrican · gtklock options (the style, modules, and wallpaper backdrop are passed by archfrican-lock).
# gtklock reads name=value under the [main] group; unknown keys are ignored, so this stays forward-safe.
[main]
time-format=%H:%M
date-format=%A, %d %B
idle-hide=true
