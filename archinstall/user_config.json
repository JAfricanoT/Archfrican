{
  "//": "BASE TEMPLATE for the ISO full install (Stage 2). lib/phase1.sh::gen_config",
  "//2": "reads this, injects the wizard's disk/encryption/hostname/timezone, and writes",
  "//3": "the live user_configuration.json. Do NOT hand-edit disk_config to a guessed 2.x",
  "//4": "schema — the authoritative shape is captured from `archinstall --dry-run` on a VM",
  "//5": "and committed (see docs/STAGE2-VALIDATION.md). Key choices: Btrfs + snapshot",
  "//6": "subvolumes, LTS kernel (linux-cachyos added in phase 2), GRUB (grub-btrfs rollback).",
  "bootloader": "Grub",
  "kernels": ["linux-lts"],
  "hostname": "archfrican",
  "audio_config": { "audio": "pipewire" },
  "network_config": { "type": "nm" },
  "profile_config": { "profile": { "main": "Minimal" } },
  "disk_config": {
    "config_type": "default_layout",
    "filesystem": "btrfs",
    "btrfs_options": {
      "snapshot_config": { "type": "Snapper" },
      "subvolumes": [
        { "name": "@",        "mountpoint": "/" },
        { "name": "@home",    "mountpoint": "/home" },
        { "name": "@log",     "mountpoint": "/var/log" },
        { "name": "@pkg",     "mountpoint": "/var/cache/pacman/pkg" },
        { "name": "@.snapshots", "mountpoint": "/.snapshots" }
      ]
    }
  },
  "packages": ["git", "vim", "sudo", "zsh", "btrfs-progs"],
  "timezone": "America/New_York",
  "swap": true
}
