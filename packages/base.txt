# Core system + reliability backbone
base-devel
git
curl
wget
zsh
btrfs-progs
snapper
snap-pac
grub-btrfs
inotify-tools          # needed by grub-btrfs daemon
reflector              # keep mirrorlist fast
pacman-contrib
zram-generator
openssh
man-db
pciutils               # lspci, for GPU auto-detection (also ensured up-front in install.sh)

# Health / hygiene + notifications (archfrican doctor, maintenance timers, FIDO2 selfcheck)
smartmontools          # SMART disk health (smartd)
fwupd                  # firmware updates
arch-audit             # CVE scan of installed packages
libnotify              # notify-send — the whole health/notify UX no-ops without it
