# Editor-agnostic toolchains + language servers (consumed by any editor).
code                   # VS Code (OSS build); frontend only
neovim                 # always handy as a fallback editor
# version managers (preferred over system languages)
rustup                 # -> rust-analyzer
go                     # -> gopls
uv                     # python toolchain + venvs
fnm                    # node version manager (modules/30-dev.sh bootstraps the LTS build)
# JS runtime + package managers (standalone binaries, not tied to fnm's node version)
bun
pnpm
# language servers (system-level, editor-independent)
gopls
rust-analyzer
pyright
ruff
typescript-language-server
clang                  # clangd for C/C++
lldb
# cli quality-of-life
ripgrep
fd
fzf
bat
eza
zoxide
atuin                  # shell history: fuzzy search + sync — wired into .zshrc's `atuin init zsh`
lazygit
github-cli                 # gh — used by archfrican-git to log in + upload the SSH key to GitHub
docker
docker-compose
postgresql             # psql/pg_dump/createdb/etc. — Arch bundles client+server in one package;
                       # this repo never enables postgresql.service, so it's client-only in practice
# lazydocker             # TUI for docker/compose — now in [extra]; or pick via archfrican-defaults
direnv
starship
