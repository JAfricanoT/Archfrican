# Compositor + Wayland desktop shell. All swappable; niri is isolated here.
niri
ghostty                # terminal (Mod+Return). Official 'extra' repo — confirm: pacman -Si ghostty
xwayland-satellite     # X11 app support inside niri
keyd                   # ⌘-style shortcut remaps for ex-mac users
sddm                   # graphical login (Qt6); themed by assets/sddm/archfrican
weston                 # tiny reference Wayland compositor that hosts the SDDM greeter (no xorg)
qt6-virtualkeyboard    # on-screen keyboard for the greeter (touch; default-off, Loader-gated in the theme)
waybar                 # status bar
fuzzel                 # launcher (styled as Spotlight)
swaync                 # notifications + control center (Quick Settings; replaces mako)
swayidle
swaylock
awww                   # wallpaper daemon (animated); swww's official successor in extra (binary: awww-daemon)
grim
slurp
wl-clipboard
cliphist               # clipboard history
brightnessctl
playerctl
pipewire
pipewire-pulse
pipewire-alsa
wireplumber
xdg-desktop-portal
xdg-desktop-portal-gtk
xdg-desktop-portal-gnome   # screencast portal that works well with niri
polkit-gnome
gnome-keyring
network-manager-applet
networkmanager             # the network DAEMON — the applet above is inert without it
nm-connection-editor       # GUI for VPNs / advanced connections
pavucontrol                # audio mixer (waybar's volume click already targets it)

# --- ecosystem: a complete native desktop (P1) ---
# File management (GUI + TUI) + thumbnails + removable-media automount
nautilus                   # GTK file manager (Finder-like, Wayland-native)
file-roller                # archive GUI
yazi                       # fast TUI file manager (Mod+Shift+Y)
gvfs                       # trash, mounts, network shares for nautilus
gvfs-mtp                   # Android/MTP devices
gvfs-afc                   # iOS devices
udiskie                    # automount removable media (spawned by niri)
tumbler                    # thumbnail service
ffmpegthumbnailer          # video thumbnails
poppler-glib               # PDF thumbnails
7zip                       # broad archive extraction (rar/7z/zip/…; official, replaces AUR unar)
xdg-user-dirs              # ~/Downloads, ~/Pictures, …
# Bluetooth
bluez
bluez-utils
blueman                    # bluetooth manager GUI
# System info / telemetry ("about this PC" + monitors)
fastfetch                  # macOS-y system info
btop                       # TUI resource monitor (Mod+Shift+B)
lm_sensors                 # temperatures (waybar thermal-zone)
nvtop                      # GPU monitor
# Power / OSD / screen-record / displays
power-profiles-daemon      # battery vs performance (waybar switch)
swayosd                    # on-screen volume/brightness feedback
wf-recorder                # screen recording (Mod+Shift+R)
nwg-displays               # GUI multi-monitor layout (niri-native; in 'extra', not AUR)
# Qt-on-Wayland (consistent look for Qt apps)
qt5-wayland
qt6-wayland
qt6ct

# --- Fase 0: quick-win UX (clipboard picker, Quick Look, privacy dot, auto light/dark, defaults) ---
sushi                      # Quick Look: select a file in nautilus + press Space for a live preview
darkman                    # auto light/dark by sun position (opt-in: `archfrican-auto-appearance on`)
geoclue                    # location provider for darkman's sunrise/sunset (used only when enabled)
jq                         # JSON for the waybar privacy (mic/cam) indicator + future launcher modes
xdg-utils                  # xdg-settings/xdg-mime — honest one-shot default-app handler (archfrican-defaults)

# --- Fase 1: launcher command-surface (calc / window-switch / file-search modes) ---
libqalculate               # qalc — the calculator behind ⌘+Shift+C (Spotlight-style "type a sum")
plocate                    # fast local file search (⌘+Shift+G); indexes ~ per-user, no root, no telemetry

# --- Fase 3: control center, VPN, pro-audio ---
wlsunset                   # night light (warm screen) — archfrican-nightlight
tailscale                  # mesh VPN (account-based) — archfrican-vpn (tailscaled enabled on first use)
wireguard-tools            # WireGuard CLI (NetworkManager GUI handles connections)
networkmanager-openvpn     # OpenVPN in the NM connection editor
networkmanager-openconnect # Cisco AnyConnect / OpenConnect in the NM connection editor
easyeffects                # mic noise suppression (RNNoise) + system EQ
qpwgraph                   # PipeWire patchbay — per-app audio routing

# --- Fase 5: accessibility (niri is the only tiling Wayland WM with a real a11y tree) ---
orca                       # screen reader — archfrican-screenreader (⌘+Alt+S)
speech-dispatcher          # speech synthesis layer for Orca
espeak-ng                  # speech voices for speech-dispatcher
